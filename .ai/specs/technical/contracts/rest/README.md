# REST API Contracts

One file per domain. Each file is the single source of truth for that domain's API.

| File | Domain | Endpoints |
|------|--------|-----------|
| `auth-api.yaml` | Auth | register, login, token refresh |
| `users-api.yaml` | Users | profile, addresses CRUD |
| `catalog/` | Catalog | categories, products, inventory |
