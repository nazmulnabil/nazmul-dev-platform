# Contracts — Integration Specifications

## Structure

```
contracts/
  rest/          OpenAPI 3.0 YAML specs per domain
  schemas/       Shared/common data schemas
  events/        Event contracts (pub/sub)
```

Each REST API spec follows the `resource-api.yaml` template.
