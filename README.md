# nazmul-dev-platform

Central context repository for all development projects.
Specs are the source of truth. Agents follow specs.

## Projects
- `django-ecommerce-api/` — Django REST API backend
- `react-ecommerce/` — React frontend (coming soon)

## Structure
- `specs/` — Feature and domain specs (source of truth)
- `agents/` — Agent instruction files (CLAUDE.md, AGENTS.md etc)
- `docs/` — Architecture decisions, conventions
